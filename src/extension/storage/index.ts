/**
 * Storage utilities - re-exports all storage functions.
 * This maintains backward compatibility with existing imports.
 */

export { AIConfig, getAIConfig, saveAIConfig, clearAIConfig } from './aiConfig';
export { getThemePreference, saveThemePreference } from './theme';

